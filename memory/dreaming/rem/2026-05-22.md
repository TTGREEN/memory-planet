# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- # 2026-05-20 工作日志 ## 今日主题：M5 完整星魂内核 推进 --- ## 一、M5 架构认知 ### Memory Planet M0→M5 路线图回顾 | 阶段 | 内容 | 状态 | |------|------|------| | M0 | atoms.db + ingest/recall + τ=10 + human_pin | ✅ | | M0.5 | recall bootstrap hook 自动注入 | ✅ | | M1 | claims/relations + GraphRAG CLI | ✅ | | M1.5 | 向量搜索（sqlite-vec + /api/vec-search） | ✅ | | M2 | Star Soul Core 独立进程（stdio IPC） | ✅ | | M3 | 矛盾检测框架（contradiction-engine） | ✅ | | M4 | PM2 部署 + REST API | ✅ | | M4.5 | 混沌工程框架（skill-sandbox + probabilistic-recall） | ✅ | | M5 | 完整星魂内核（熵减驱动 + 因果拓扑 + 范式转移） | 🔄 进行中 | ### M5 四大高维节点（仕泽白皮书 v2） 1. **节点一**：全息概率态（embedding 概率云，ECV 概率态平替） 2. **节点二**：分形因果拓扑（CAUSE/EFFECT 强有向边） 3. **节点三**：熵减驱动自创生引擎（已实现 ✅） 4. **节点四**：跨维技能投射（Pr [confidence=0.71 evidence=memory/daily-logs/2026-05-20.md:1-41]
- 2. **CLAIMS 数据质量** — 当前 claims predicate 大量中文（"正在研究"/"推进"/"喜欢"），不是结构化的 CAUSE/EFFECT，因果拓扑 builder 跑完无 causal links 3. **Paradigm shift 存储** — 生成后已写入 atoms.db，但还未通过 `relations` 链接到源 atom（代码有，但可能因失败跳过） ### 建议方向 1. **增加 dream-entropy timeout** — 当前 120s 不够，建议 180s 或改用 sessions_spawn 后台跑 2. **跑 causal-topology-builder** — 对高 importance atoms 补充 CAUSE 类 claims，增加 claim 多样性 3. **手动注入一些真正矛盾的 claims** — 比如"简单性 vs 扩展性"、"同步 vs 异步"的 CAUSE/CAUSES 对立 4. **观察 5 对矛盾种子** — 等待 cron 触发 dream-entropy 看是否有真正的 paradigm shift 被触发 --- ## 六、核心教训 1. **MiniMax thinking_enabled 行为**：发送 `false` 和不发送效果完全不同，这是 Anthropic API 兼容层的陷阱 2. **judge prompt 要简洁**：多行解释性 prompt 反而让模型输出解释而非直接答案 3. **M5 需要真实矛盾素材**：atoms 之间相似度高时无法 [confidence=0.71 evidence=memory/daily-logs/2026-05-20.md:123-146]
- 2. **judge prompt 要简洁**：多行解释性 prompt 反而让模型输出解释而非直接答案 3. **M5 需要真实矛盾素材**：atoms 之间相似度高时无法触发真正冲突，需要主动注入对立的架构原则 --- ## 七、M5 断点续传架构落地 ### 问题：SIGKILL 根因 dream-entropy 跑到一半被系统 SIGKILL，不是 timeout 太短的问题，而是**脚本思维的天然限制**： - cron 的 agentTurn 在 120s 后强制 kill - exec 所有输出缓存在内存里，LLM 长文本撑爆 buffer - 进程树退出时所有子进程一起死 ### 解决方案：spawn + SQLite 状态机 仕泽提出"操作系统守护进程思维"——用断点续传 + detached worker 重构。 **evolution_tasks 表：** ```sql CREATE TABLE evolution_tasks ( id TEXT PRIMARY KEY, subject TEXT NOT NULL, pair_data TEXT NOT NULL, -- JSON pairs status TEXT DEFAULT 'PENDING', -- PENDING / PROCESSING / COMPLETED / FAILED result_data TEXT, error_msg TEXT, created_at TEXT, updated_ [confidence=0.71 evidence=memory/daily-logs/2026-05-20.md:138-176]
